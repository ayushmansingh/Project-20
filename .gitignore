# dependencies (Netlify installs these on deploy)
node_modules/

# local Netlify state
.netlify

# OS cruft
.DS_Store
**/.DS_Store

# local secrets (never commit)
.gemini_key
.env
key.txt

# source photo dump (originals, HEIC, zip) — keep out of repo
Images/
